#include <stdio.h>

void uploadVideo() {
    // Lógica de carga de video aquí
}

void playVideo(int videoId) {
    // Lógica de reproducción de video aquí
    printf("Reproduciendo video con ID: %d\n", videoId);
}

int main() {
    uploadVideo();
    playVideo(1234);
    return 0;
}
